import React from "react";

const MockComponent = (props) => {
    return (
        <div>
            Mock Component:
            {props.message}
        </div>
    )
}

export default MockComponent;
